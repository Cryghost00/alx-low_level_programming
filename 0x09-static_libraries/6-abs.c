#include "main.h"
/**
 * _abs - A program that computes the absolute value of an integer
 * @n: integer to check
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
